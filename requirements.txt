fastapi>=0.100.0,<0.112.0 # Pinning minor version for stability
uvicorn[standard]>=0.20.0,<0.30.0
sqlalchemy[asyncio]>=2.0.0,<2.1.0 # For async support
asyncpg>=0.25.0,<0.30.0 # PostgreSQL async driver
pydantic>=2.0.0,<2.8.0
pydantic-settings>=2.0.0 # For settings management, though not heavily used here
redis[hiredis]>=5.0.0,<5.1.0 # Async redis client
faker>=15.0.0,<26.0.0 # For generating fake data
httpx>=0.24.0,<0.28.0 # For making HTTP requests to the mock API
psycopg2-binary # Often needed as a fallback or for certain SQLAlchemy sync operations, even with asyncpg
# alembic # If you were to add database migrations